func main {
    pop

    &test call_it
    { "LAMBDAS WORK TOO \n" puts } &fcall call_it
    { "This is a lambda, being called from " 4 _last_word _string strconcat puts "\n" puts } call_it 

    0
}

func call_it { fcall }

func test {
    "WORKS\n" puts
}
